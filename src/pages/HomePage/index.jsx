import { LoginForm } from "../../components/forms/LoginForm"

export const HomePage = () => {
    return(
        <main>
            <h1>Home Page</h1>
            <LoginForm />
        </main>
    )
}